# Frontend

UI code is under `app`, reusable controls under `components`, and browser helpers under `lib`. Assets and the Sakha brochure are under `public`.

Run commands from the project root: `pnpm dev`, `pnpm build`. Do not run a second dependency installation here. `app/api` contains only route bridges; their implementations are in `../backend/api`.
